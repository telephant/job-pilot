import path from 'path';

// Use absolute paths for more reliable file resolution
export const CREDENTIALS_PATH = path.resolve('./credentials.json');
export const TOKEN_PATH = path.resolve('./token.json');
